function checkprime(num) {
  let count=0;
  for(let i=0;i<=num;i++){ 
   if(num%1==0){
      count++;
   }
  }
      if(count==2){
        console.log(num," is Prime");
        
    } if(count>2)   {
      console.log(num,"is Not");
   
  }
checkprime(15)